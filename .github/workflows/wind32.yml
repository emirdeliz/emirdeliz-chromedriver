name: Windows 32
on: [push]
jobs:
  test:
    runs-on: windows-latest-8-cores
    steps:
      - uses: actions/checkout@v3
      - uses: ./
        with:
          download-path: temp
          version: 110.0.5481.30
          arch: wind32
